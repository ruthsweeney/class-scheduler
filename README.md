# Class Scheduler MVP

Glofox flexible class scheduling prototype.

Built with React (CDN), Babel Standalone, and inline CSS — no build step required.

## Deploy to Vercel

See deployment instructions below, or run:

```bash
npx vercel
```
